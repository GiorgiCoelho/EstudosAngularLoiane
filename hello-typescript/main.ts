var minhaVariavel = 'minha variável';

function soma (x, y) {
    return x + y;
}

let num = 2;
const PI = 3.14;

let numeros = [1,2,3];
numeros.map(numero => numero * 2);

class Matematica {
    somar (x, y) {
        return x + y;
    }
}

let variavelTipadaExemplo: number = 1;
/* variavelTipadaExemplo = '123';  ERRO! VARIÁVEL É DO TIPO INTEGER. TS NÃO COMPILA **/
